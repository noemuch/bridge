---
type: migration
reason: {{reason}}
date: {{formatDate date}}
from: "{{from}}"
to: "{{to}}"
severity: {{severity}}
deprecated-at: {{formatDate deprecatedAt}}
{{#if removalAt}}removal-at: {{formatDate removalAt}}{{/if}}
---

# Migration: `{{from}}` → `{{to}}`

> Generated by Bridge Docs cron {{formatDate date}} from KB diff v{{fromKbVersion}} → v{{toKbVersion}}.

## Reason

{{reason-body}}

## Affected DS docs (auto-updated)

{{#each affected}}- [{{name}}]({{path}})
{{/each}}

## Affected consumer code

Run in your repo:

```bash
rg --type tsx --type jsx '{{from}}'
```

## Migration steps

{{#each steps}}{{@index}}. {{this}}
{{/each}}

## Timeline

- Deprecated: {{formatDate deprecatedAt}}
{{#if removalAt}}- Removed: {{formatDate removalAt}}{{/if}}
